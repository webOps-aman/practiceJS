let num = 10.1;
let result = Math.ceil(num);
console.log(result);

// without decimal give base value......agar jaise he decimal add ke bhd usme value add kerte hia decimal mai to wo phir uski upper value return kerta hai 

// work only in decimal value